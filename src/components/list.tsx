import * as React from 'react';

export interface ListComponentProps {
}

export default class ListComponent extends React.Component<ListComponentProps, any> {
  public render() {
    return (
      <div>
        
      </div>
    );
  }
}
